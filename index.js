const num = 23;

const str = 'string';

const bool = false;

const msgOnMain = 'msg on main';

const obj = {
    age: 29,
    name: 'Кирилл',
    isAdmin: true,
};

const msg1 = 'msg1';
const msg2 = 'msg2';

const errorFn = () => {};
const fixMsg = "fix_msg";
const oneMoreMsg = "one_more_message";
const h = 22;

const newMsg = 'new_msg';
const newMsg2 = 'new_msg_2';

